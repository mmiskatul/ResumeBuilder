import Resum from '../models/resumeModel.js'


export const createResum=async (req,res)=>{
    try {
        const {title}=req.body;
        // DEFAULT TEMPALTE 
    } catch (error) {
        
    }
}